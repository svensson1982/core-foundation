name: Sync Repos

on:
  push:
    branches:
      - main
    paths: 
      - 'tokens/**'
      - '!.github/workflows/**' 

jobs:
  sync-core-web:
    uses: ./.github/workflows/sync-repo.yml
    with:
      target_repo: "core-web"
      foundation_repo: "core-foundation"

  sync-core-android:
    uses: ./.github/workflows/sync-repo.yml
    with:
      target_repo: "core-android"
      foundation_repo: "core-foundation"

  sync-core-ios:
    uses: ./.github/workflows/sync-repo.yml
    with:
      target_repo: "core-ios"
      foundation_repo: "core-foundation"
        
